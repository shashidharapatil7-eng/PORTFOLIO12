/**
 * InsightForge — firebase-config.js (LOCAL TESTING ONLY)
 * ---------------------------------------------------------------
 * 1. Copy this file to js/firebase-config.js  (that filename is
 *    gitignored, so your keys never get committed).
 * 2. Paste your project's values below — Firebase Console →
 *    Project settings → General → "Your apps" → SDK setup and
 *    configuration → Config.
 *
 * On Netlify, this exact file is generated automatically at build
 * time from environment variables (see scripts/build-config.js and
 * README.md) — you do not need to upload it yourself.
 * ---------------------------------------------------------------
 */
window.FIREBASE_CONFIG = {
  apiKey: "YOUR_API_KEY",
  authDomain: "YOUR_PROJECT_ID.firebaseapp.com",
  projectId: "YOUR_PROJECT_ID",
  storageBucket: "YOUR_PROJECT_ID.appspot.com",
  messagingSenderId: "YOUR_SENDER_ID",
  appId: "YOUR_APP_ID",
};
